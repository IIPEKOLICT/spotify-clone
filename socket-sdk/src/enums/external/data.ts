export enum SocketPlace {
  USER_PAGE = 'user-page',
}

export enum SocketEntity {
  USER_STATUS = 'user-status',
  POST = 'post',
  AUDIO = 'audio',
}

export enum SocketScope {
  ENTITY = 'entity',
  USER = 'user',
}
