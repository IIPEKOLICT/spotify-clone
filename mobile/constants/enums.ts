export enum HttpMethod {
  GET = 'GET',
  POST = 'POST',
  PATCH = 'PATCH',
  PUT = 'PUT',
  DELETE = 'DELETE',
  OPTIONS = 'OPTIONS',
  HEAD = 'HEAD',
  ANY = 'ANY',
}

export enum AuthType {
  LOGIN = 'Login',
  REGISTRATION = 'Registration',
}

export enum RoutePath {
  HOME = 'Home',
  LOGIN = 'Login',
  REGISTRATION = 'Registration',
}
