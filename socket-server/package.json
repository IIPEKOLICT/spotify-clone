{
  "name": "socket-server",
  "version": "1.0.0",
  "scripts": {
    "dev": "nodemon",
    "build": "rm -rf dist && tsc",
    "prod": "npm run build && node dist/socket-server/src/main.js"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "socket.io": "^4.6.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
